import React from 'react'
import PlayerInterface from '../components/PlayerInterface';

function Play(props) {
    return (
        <div>
            <PlayerInterface />
        </div>
    );
}

export default Play;